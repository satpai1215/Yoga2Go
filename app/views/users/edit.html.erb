<h1>Update Your Email Address</h1>

<div class="row">
	<div class="span10">
		<%= form_for(@user) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "Save Changes", class: 'btn btn-large btn-primary' %>
		<% end %>
	</div>
</div>	