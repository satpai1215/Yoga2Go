<h1>Create Flashcard</h1>
<%= link_to "Back to Deck", @deck %>

<div class="row">
	<div class="span10">
		<%= form_for [@deck, @flashcard] do |f| %>
			<%= render 'fields', f: f %>
			<div><%= f.submit "Create Flashcard", class: 'btn btn-large btn-primary' %></div>
		<% end %>
	</div>
</div>	